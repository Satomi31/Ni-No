<h4>電話番号詳細</h4>

<table>
  <tbody>
    <tr>
      <td>電話番号</td>
      <td><%= @phone_number.telephone_number %></td>
    </tr>
    <tr>
      <td>販売ステータス</td>
      <td><%= @phone_number.sale_status_i18n %></td>
    </tr>
  </tbody>
</table>

<%= link_to '編集する', edit_admin_phone_number_path(@phone_number) %>

<h4>契約状況</h4>

<table>
  <thead>
    <th>ID</th>
    <th>会社名</th>
    <th>ステータス</th>
  </thead>
  <tbody>
    <% @contracts.each do |contract| %>
      <tr>
        <td><%= contract.id %></td>
        <td>
          <%= link_to admin_customer_path(contract.customer.id) do %>
            <%= contract.customer.company_name %>
          <% end %>
        </td>
        <td>
          <% if contract.is_under_contract == true %>
            契約中
          <% else %>
            解約済
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>